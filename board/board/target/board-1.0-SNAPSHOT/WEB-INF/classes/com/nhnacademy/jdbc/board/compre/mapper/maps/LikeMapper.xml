<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nhnacademy.jdbc.board.compre.mapper.LikeMapper">
    <select id="userLike" resultType="com.nhnacademy.jdbc.board.compre.dto.LikeDTO">
        SELECT like_no AS likeNo, post_no AS postNo, user_no AS userNo
        FROM Likes
        WHERE post_no = #{postNo} AND user_no = #{userNo}
    </select>

    <insert id="likeInsert">
        INSERT INTO Likes(post_no, user_no)
        VALUES (${postNo}, ${userNo})
    </insert>

    <delete id="likeDelete">
        DELETE FROM Likes WHERE post_no=${postNo} AND user_no=${userNo}
    </delete>
</mapper>